#include <stdio.h>

#include <unistd.h>
write(stream, data, size);

Integer Value, Symbolic Constant,	Standard I/O Stream,	Description
0	STDIN_FILENO	stdin	Standard Input
1	STDOUT_FILENO	stdout	Standard Output
2	STDERR_FILENO	stderr	Standard Error

#include <stdlib.h>


#include <fcntl.h>

// pointer function declaration
returnval (*functionName)(parameters);
functionName = &referencedFunctionName; 
