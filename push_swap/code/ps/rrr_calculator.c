
#include "ps.h"

/* int calculate_rrr_cost()
{
  int a_index;
  t_dlist *i;

  i = a -> end;
  a_index = 0;
  while (i && a_index <= (a -> e_count / 2))
  {
    cost = a_index + calculate_b_move_rrr(a -> target, b);
    a_index++;
    i = i -> prev;
  }
} */