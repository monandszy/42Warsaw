#include "fract_ol.h"

void zoom_in(t_data *d, int x, int y)
{
  (void) x;
  (void) y;
  (void) d;

  // render(d, &calculate_mandelbrot_depth);
}

void zoom_out(t_data *d, int x, int y)
{
  (void) x;
  (void) y;
  (void) d;
}